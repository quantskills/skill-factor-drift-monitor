# Local synthetic test data

Run `python generate_synthetic_panel.py` to create `synthetic_panel.csv`. The generated panel is deterministic and contains no real securities or market observations. Generated CSV, Parquet, and report files are ignored by Git.
